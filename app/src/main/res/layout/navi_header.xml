<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="160dp"
    android:background="#2D3365">

    <ImageView
        android:id="@+id/headerUserImage"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_gravity="center"
        android:scaleType="centerCrop"
        android:src="@drawable/outline_person_24"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.103"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/headerUserEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="20dp"
        android:layout_marginTop="40dp"
        android:text="abc@abc.com"
        android:textColor="@color/white"
        app:layout_constraintStart_toEndOf="@+id/headerUserImage"
        app:layout_constraintTop_toTopOf="@+id/headerUserImage" />

    <Button
        android:id="@+id/headerLogoutBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="Logout"
        app:layout_constraintBottom_toBottomOf="@id/headerUserImage"
        app:layout_constraintStart_toStartOf="@id/headerUserEmail"
        app:layout_constraintTop_toTopOf="@id/headerUserEmail" />


</androidx.constraintlayout.widget.ConstraintLayout>